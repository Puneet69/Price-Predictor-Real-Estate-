services:
  - type: web
    name: property-comparison-backend
    env: python
    plan: free
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: MONGODB_URI
        value: mongodb+srv://price_predictor:vlMUA2FIr48bnJWO@realestate.caqfzde.mongodb.net/property_comparison?retryWrites=true&w=majority&appName=RealEstate
      - key: PYTHON_VERSION
        value: "3.11"

# Note: Static sites must be deployed separately through Render dashboard
# Frontend deployment:
# 1. New + → Static Site
# 2. Root Directory: frontend  
# 3. Build Command: npm ci && npm run build
# 4. Publish Directory: build
# 5. Environment Variable: REACT_APP_API_URL=https://property-comparison-backend.onrender.com